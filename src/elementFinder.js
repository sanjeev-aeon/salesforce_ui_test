/**
 * Find element which is uniquely identified by selector and can be reached using XPath in the provided DOM.
 * @param {String} selector 
 * @param {String} xpath- xpath represent a path to reach to the element which is identified by provided selector
 * @return the first element 
 */
const findElement=(selector,xpath, theDOM)=>{

}

